# 화이트리스트 방식: 전부 무시하고 하네스 파일만 추적한다.
# 새 파일을 동기화 대상에 넣으려면 아래에 !경로 를 추가한다.
/*

# --- 동기화 대상 (하네스) ---
!.gitignore
!CLAUDE.md
!settings.json
!protected-branches.json
!package.json
!hooks/
!skills/
!agents/
!commands/
!templates/

# --- 화이트리스트 폴더 안에서도 제외할 것 ---
# (현재 없음 — 캐시·로그성 파일이 생기면 여기에 추가)
