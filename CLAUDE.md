# Rathon R&D — 전역 가이드

> 모든 프로젝트 공통. 프로젝트별 세부(빌드 명령, 주의사항)는 각 저장소의 CLAUDE.md에 둔다.
> Karpathy 플러그인 가이드라인(단순성, 외과적 변경)과 함께 동작한다.

## 응답
- 항상 한국어로 응답한다. 코드 주석도 한글로 작성한다.

## 작업 방식 (가장 중요)
- **비판 의무**: 요구사항·설계에 문제(보안, 성능, 유지보수, 더 나은 대안)가 보이면
  구현 전에 반드시 지적한다. 무조건 동의 금지. 대안은 트레이드오프와 함께 제시한다.
- **범위 준수**: 변경은 요청한 것에 한정한다. 요청 밖 리팩터링·수정 금지.
  작업 중 발견한 다른 문제는 고치지 말고 보고만 한다.
- **검증 후 완료**: 완료 선언 전 반드시 빌드로 확인한다. 테스트가 있으면 실행하고,
  실패하면 실패했다고 보고한다. "될 것 같다" 금지.
- **테스트 동반**: 신규 기능·신규 프로젝트는 테스트 코드를 함께 작성한다.

## 코딩 규칙
- 기본 스택: Java 21 / Spring Boot / Gradle. 프론트 React, 레거시 화면 JSP.
- 메서드 45줄 이하. 초과하면 분리한다.
- Lombok 사용 (@Getter, @Builder, @RequiredArgsConstructor). DTO/VO는 record.
- 흐름 제어에 try-catch 금지. null 반환 금지 — Optional 사용.
- 쿼리는 JPA/MyBatis 파라미터 바인딩 필수. 문자열 연결 쿼리 절대 금지.
- 로그에 자격증명·개인정보 노출 금지 (마스킹).
- 기존 파일 수정 시 그 파일의 스타일·패턴·네이밍을 따른다. 새 스타일 도입 금지.

## Git
- 보호 브랜치(main 등) 직접 커밋·push 금지. feature/* fix/* site/* 브랜치에서 작업.
  목록: ~/.claude/protected-branches.json — branch-gate 훅이 위반 시 확인을 요구한다.
- 커밋: <타입>(스코프): [사이트명] <설명> — Conventional Commits.
  사이트 커스텀 시 스코프가 docs/site-custom/[사이트]/[스코프].md 문서명이 된다.
- 사내 Gitea는 PR 게이트가 없다 → 보호 브랜치로 머지하기 전 반드시
  변경 diff 요약을 보고하고 사용자 확인을 받는다.

## 사이트 커스텀
- 기존 클래스 상속 + 프로필 교체 방식. 원본 클래스 직접 수정 금지.
- site/* 브랜치 커밋 시 문서화 훅이 자동 동작한다. 수동 실행: /site-doc
- **사이트 작업은 해당 사이트 worktree에서 구현한다**: git worktree list로 경로를 찾아
  에이전트 지시서에 절대경로로 지정한다 (세션 폴더와 무관하게 작업 가능).
  사이트별 application.DEV.yml·IDE 설정이 그 폴더에 있어 사용자가 바로 기동 확인한다.
  해당 사이트 worktree가 없으면 생성을 제안한다.
  사이트 작업 때문에 메인 개발 폴더의 체크아웃 브랜치를 바꾸지 않는다.

## 워크플로
- 기능 개발 사이클(설계 대화→위임→구현→리뷰→검증→보고)은 /work 사용.
  메인 세션은 설계와 보고 수신, 실제 작업은 서브에이전트가 수행한다.
- 코드 리뷰만 따로: /review. 컨벤션 커밋: /commit.
- **에이전트 라우팅 기본값**: 스킬을 명시하지 않아도 메인 세션이 작업 유형을 보고
  적절한 에이전트로 위임한다 — 원인 분석 analyzer / 구현 implementer /
  리뷰 code-reviewer / 검증 verifier / 광범위 코드 탐색 Explore.
  메인 세션 직접 작업은 요구사항 정리·에이전트 지시서 작성·결과 보고·초경량 조회에 한정.
- **백그라운드 위임 필수**: 에이전트는 run_in_background로 던지고 즉시 턴을 종료해
  메인 세션을 사용자에게 돌려준다. 완료 알림을 받으면 결과를 보고하고,
  후속 단계(구현→리뷰→검증)가 있으면 다음 에이전트를 이어서 백그라운드로 위임한다.
- **에이전트 보고서 위키 저장**: 에이전트 완료 보고 전문은 채팅에 쏟지 않고 위키에 보관한다.
  - 저장 위치: `<사용자 홈>\wiki\에이전트보고\` 전용 폴더(없으면 생성). 위키 폴더 자체가
    없는 머신이면 `<사용자 홈>\.claude\reports\`로 폴백 저장한다. 파일명은
    `YYYY-MM-DD_HHmm_<작업명-kebab>.md`. 작업로그(`wiki\작업로그\YYYY-MM.md`)와 역할을
    분리한다 — 작업로그엔 요약 1줄+보고서 링크, 보고서 폴더엔 전문.
  - 저장 주체: Write 보유 에이전트는 직접 파일로 저장하고 반환은 한 줄 요약+경로만 한다.
    완료 알림 수신 시 메인 세션은 반환 경로의 파일 존재를 확인하고, 없거나 에이전트가
    전문을 그대로 반환했으면 메인이 받은 전문을 위 포맷으로 직접 저장한다(폴백).
  - 채팅 출력: 완료 알림은 `<아이콘> [에이전트] 작업명 — 상태: 한 줄 요약 (보고서: 경로 → /reports N)`
    형식의 한 줄로만 표출한다(2줄 초과 금지). 열람·삭제·정리는 /reports 스킬로 한다.
  - **예외(즉시 채팅 표출)**: critical 발견(보안·중대 버그)·차단 이슈(빌드 실패·verifier 불합격)·
    사용자 입력 필요(질문 복귀, 권한·결정 대기)는 문서로만 두지 말고 핵심 내용을 채팅에 바로
    띄운다. 이 경우에도 보고서 파일은 함께 저장하고 frontmatter `status`에 차단/needs-input을 적는다.
  - 정리: 자동 삭제는 하지 않는다(수동). /reports 목록 조회 시 읽음 처리된·오래된(30일+) 보고서는
    "정리할까요?" 제안만 표시한다.
